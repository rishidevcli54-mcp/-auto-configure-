{
  "name": "auto-configure-vscode",
  "displayName": "Auto Configure (dev)",
  "publisher": "rishidevcli54-mcp",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.70.0"
  },
  "description": "Helper modules for VS Code TypeScript/TS Server integration (development).",
  "main": "out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "pretest": "npm run build",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.70.0"
  },
  "dependencies": {
    "semver": "^7.3.8",
    "vscode-uri": "^3.0.0"
  }
}
